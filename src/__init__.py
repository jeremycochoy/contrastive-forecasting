# src/ package — import submodules explicitly to avoid eager dependency loading.
# Usage: from src.models import ConfigurableModel
#        from src.recovery import create_recovery_head
#        from src.arma import generate_arma_batch
